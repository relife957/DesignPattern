package Proxy;

/**
 * 代理模式(Proxy)的意义其实就是代理的意义,之所以使用代理,就是因为但是人不便出面,只能请代理出面解决,在编程中就是需要一个局部的对象行使原对象的功能
 */
public interface GiveGift {
    void giveDolls();
    void giveFlowers();
    void giveChololate();
}
